server:
  port: 8070

eureka:
  instances:
    hostname: localhost
  client:
    fetchRegistry: false
    registerWithEureka: false
    serviceUrl:
      defaultZone: http://${eureka.instances.hostname}:${server.port}/eureka/
